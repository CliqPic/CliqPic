version: '2'

services:
  server:
    build: .
    image: amdirent/cliq-pic:0.0.66
    network_mode: 'host'
    env_file: docker-compose.env
    ports:
      - '30.0.6600'

  sidekiq:
    build: .
    image: amdirent/cliq-pic:0.0.66
    network_mode: 'host'
    env_file: docker-compose.env
    command: [bundle, exec, sidekiq]
